'use strict';

var P = require('bluebird');

module.exports = initializeDatabase;

/*--------------------------------------------------------------------------------*/

function initializeDatabase(config) {
  return P.resolve();
}
